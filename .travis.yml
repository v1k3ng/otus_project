---
dist: trusty
sudo: required
language: bash
before_install:
  - echo "====================================="
  - pwd
  - cd docker/search_engine_crawler
  - echo "====================================="
  - ls -la
  - echo "====================================="
  - echo "Test is OK"
  - echo "====================================="
# - curl https://raw.githubusercontent.com/v1k3ng/otus_project/test/travis.sh | bash
# - curl https://raw.githubusercontent.com/express42/otus-homeworks/2019-02/run.sh | bash
